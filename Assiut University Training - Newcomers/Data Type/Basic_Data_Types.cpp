#include<bits/stdc++.h>
using namespace std;
#define endl "\n"
#define fast ios_base::sync_with_stdio(false);cin.tie(nullptr);cout.tie(nullptr)

int main(void)
{   
    fast;
    int X;
    long long Num;
    char ch;
    float f;
    double d;
    cin >> X >> Num >> ch >> f >> d;
    cout << X << endl << Num << endl << ch << endl << f << endl << d;
    return 0;
}