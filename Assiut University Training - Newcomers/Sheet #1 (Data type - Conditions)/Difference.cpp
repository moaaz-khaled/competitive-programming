#include<bits\stdc++.h>
using namespace std;
int main(){
    long long int a,b,c,d;
    cin >> a >> b >> c >> d;
    long long int Difference = (a * b) - (c * d);
    cout << "Difference = " << Difference;
    return 0;
}